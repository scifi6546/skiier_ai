use nalgebra::Vector2;
use petgraph::Graph;
fn grid_graph(size: usize) -> Graph<Vector2<i32>, f32> {
    todo!()
}
pub fn g() {}
#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
